# SPECIFICATION.md — gas-badminton-scheduler

作成：2026-06-01  
位置づけ：REQUIREMENTS.md（何を作るか）の次の層。**どう動くか・どう見えるか**を定義する。

> **読む前に**
> - 「何を作るか・なぜ作るか」は [REQUIREMENTS.md](./REQUIREMENTS.md) を参照
> - 用語の定義は [GLOSSARY.md](./GLOSSARY.md) を参照
> - 設計判断の経緯は [DECISION_NOTES.md](./DECISION_NOTES.md) を参照

---

## この仕様書の使い方

| セクション | 誰が使うか | タイミング |
|:--|:--|:--|
| データモデル詳細 | 実装者 | スプレッドシート設計・GASコーディング前 |
| 開放パターン定義 | 実装者 | スクレイピング検証後に記入 → F1 実装前 |
| LIFF 画面仕様 | 実装者・レビュアー | HTML 実装前 |
| 通知文面カタログ | 実装者 | LINE 通知の実装前 |
| 処理フロー詳細 | 実装者 | 各機能の実装前 |

---

## 1. データモデル詳細

> REQUIREMENTS.md のデータモデルセクションに「概念」を書いた。このセクションには「列名・型・制約」を書く。

### 1-1. schedules シート（スケジュール情報）

| 列名 | 型 | 制約 | 説明 |
|:--|:--|:--|:--|
| date | String（YYYY-MM-DD） | NOT NULL | 利用日 |
| start_time | String（HH:MM） | NOT NULL | 開始時刻 |
| end_time | String（HH:MM） | NOT NULL | 終了時刻 |
| facility_name | String | NOT NULL | 施設名（例：鳥屋野総合体育館） |
| pattern | String（A/B/C/D） | NOT NULL | 開放パターン（詳細はセクション2参照） |
| scraped_at | Datetime | NOT NULL | スクレイピング実行日時 |
| floor_map_url | String | NULL 許可 | フロアマップ画像URL（D-022参照）。IMPORTXML で予約ページから動的取得した値を保存する。 |

> ⚠️ 主キー候補：（date・start_time・facility_name）の組み合わせ。重複チェックはこの3列で行う。
> ⚠️ `end_time` の計算ロジック（亀田のみ）：施設HPに終了時刻の記載がないため、F1スクレイピングで以下のルールで計算して埋める。
> - 同じ日に複数パターンがある場合：次のパターンの開始時刻を終了時刻とする
> - 最後のパターン（その日の最終スロット）：平日 = 21:00、日曜・祝日 = 17:00

---

### 1-2. responses シート（アンケート回答）

| 列名 | 型 | 制約 | 説明 |
|:--|:--|:--|:--|
| line_user_id | String | NOT NULL | 回答者の LINE ユーザーID |
| date | String（YYYY-MM-DD） | NOT NULL | 回答した日付スロット |
| start_time | String（HH:MM） | NOT NULL | 回答した開始時刻スロット |
| answer | String（going/maybe） | NOT NULL | going = 行ける / maybe = 未定 |
| answered_at | Datetime | NOT NULL | 回答日時 |
| notified_at | Datetime | NULL 許可 | 4人到達通知の送信日時（NULL = 未通知） |

> ⚠️ `notified_at` が NULL でないスロットへの pushMessage は送信しない（重複通知防止）。
> ⚠️ 主キー候補：（line_user_id・date・start_time）の組み合わせ。1人が同じスロットに2回回答した場合は上書きする。

---

### 1-3. queue シート（予約キュー）

| 列名 | 型 | 制約 | 説明 |
|:--|:--|:--|:--|
| line_user_id | String | NOT NULL | 予約者の LINE ユーザーID |
| date | String（YYYY-MM-DD） | NOT NULL | 予約希望日 |
| start_time | String（HH:MM） | NOT NULL | 予約希望開始時刻 |
| facility_name | String | NOT NULL | 希望施設名 |
| court | String | NOT NULL | 希望コート名（優先順位ロジック適用後の確定値） |
| unlock_date | String（YYYY-MM-DD） | NOT NULL | 予約解禁日 |
| status | String | NOT NULL | waiting / done / failed |
| queued_at | Datetime | NOT NULL | キュー登録日時 |
| processed_at | Datetime | NULL 許可 | 処理完了日時（NULL = 未処理） |

---

### 1-4. reserver-master シート（メンバー管理）

| 列名 | 型 | 制約 | 説明 |
|:--|:--|:--|:--|
| line_user_id | String | NOT NULL・主キー | LINE ユーザーID |
| display_name | String | NOT NULL | LINEグループ上の表示名 |
| full_name | String | NOT NULL | 氏名（予約フォームに使用） |
| user_type | String（reserver/answer_only） | NOT NULL | reserver = 予約者 / answer_only = 回答のみ |
| phone | String | user_type=reserver のとき NOT NULL | 電話番号 |
| email | String | user_type=reserver のとき NOT NULL | メールアドレス |
| court_priority | String（JSON） | NULL 許可 | コート優先順位（詳細は下記） |
| registered_at | Datetime | NOT NULL | 登録日時 |
| last_answered_at | Datetime | NULL 許可 | 最終回答日時（90日チェック用） |

**court_priority の形式：**

> ✅ D-020（2026-06-02）でコート名が確定した。施設ごとにキーを持つ JSON を1セルに格納する。

```json
// 施設ごとにキーを持つ JSON（1セルに格納）。配列の先頭が第1希望。
// コート名は予約ページの正式名称（全施設「バドミントンX」形式）で統一（2026-06-02 実機確認済み）。
{
  "東総合スポーツセンター": ["バドミントン1", "バドミントン2", "バドミントン3", "バドミントン4"],
  "鳥屋野総合体育館": ["バドミントン1", "バドミントン2", "バドミントン3", "バドミントン4", "バドミントン5", "バドミントン6"],
  "亀田総合体育館": ["バドミントン9", "バドミントン10", "バドミントン11", "バドミントン1", "バドミントン2", "バドミントン3", "バドミントン4", "バドミントン5", "バドミントン6", "バドミントン7", "バドミントン8"]
}
```


> ⚠️ 亀田の court_priority には全11コートを含める（バドミントン3・4・7・8はDパターン開放時に使用可能）。実装側でパターンに応じて使用可能コートをフィルタリングしてから優先順位を適用すること（各パターンの使用可能コートはセクション2参照）。

---

### 1-5. settings シート（施設状態管理）

> D-021（2026-06-02）で「施設状態はスプレッドシートの settings シートで管理する」と決定。コード変更なしで施設の追加・状態変更が可能になる。

| 列名 | 型 | 制約 | 説明 |
|:--|:--|:--|:--|
| facility_id | String | NOT NULL・主キー | 施設ID（例：413、420、429、442） |
| facility_name | String | NOT NULL | 施設名（例：東総合スポーツセンター） |
| status | String | NOT NULL | 施設状態。`active`（通常）/ `under_renovation`（工事中）/ `disabled`（無効）の3種 |
| last_checked_at | Datetime | NULL 許可 | 最終スクレイピング確認日時 |
| notes | String | NULL 許可 | 備考（例：「工事中 2026-04-01〜2027-03-31」） |

**初期データ（2026-06-02 時点）：**

| facility_id | facility_name | status |
|:--|:--|:--|
| 413 | 東総合スポーツセンター | active |
| 420 | 鳥屋野総合体育館 | active |
| 429 | 亀田総合体育館 | active |
| 442 | 西総合スポーツセンター | under_renovation |

> ⚠️ 西総合（442）は 2026-04-01〜2027-03-31 まで工事中。GAS が2週間に1回スケジュールを確認し、「○」が出現したら管理者に通知する（D-021 参照）。

---

## 2. 開放パターン定義

> 確定日：2026-06-02（D-020 の実機調査結果を転記）

施設ごとに「体育室のどの組み合わせが使えるか」をパターン A/B/C/D で分類する。

### 2-1. 鳥屋野総合体育館

**スケジュールページのセル表記形式**: `"9-13大体育室 A / 13‐21中体育室 B"`

> ⚠️ ハイフンが半角 `-`（U+002D）と Unicode `‐`（U+2010）の2種類混在。正規表現でパースする際は両方を考慮すること。

| パターン | 使える体育室 | バドミントン面数 | コート番号 | 時間帯 |
|:--|:--|:--|:--|:--|
| A | 大体育室 | 6面 | バドミントン1〜6 | 9〜13時 |
| B | 中体育室 | 3面 | バドミントン1〜3（4〜6は選択不可） | 13〜21時 |

---

### 2-2. 東総合スポーツセンター

**スケジュールページのセル表記形式**（実サイト確認済み・2026-06-02）:
- 時間指定あり（複数パターン）: `"9-17時 A、17-21時 B"` （`、`読点区切り）
- 時間指定あり（3パターン）: `"9-13時 B、13-15時 C、15-21時 A"` （木曜日等）
- 時間指定なし（終日）: `"B"` または `"A"` のみ → D-025 ルールで 09:00〜21:00（日祝17:00）
- 部分利用不可: `"9-11時 B、11-21時 ×"` （`×`部分はスクレイパーが自動スキップ）

> ⚠️ A=サブアリーナ・B=メインアリーナ（順序が直感と逆。Aの方が面数が少ない）。誤解しやすいため注意。
> ⚠️ 区切り文字は `、`（読点）。IMPORTHTML 経由では改行になる場合あり。パーサーはグローバル正規表現で対応済み（D-026）。

| パターン | 使える体育室 | バドミントン面数 | コート番号 | 時間帯 |
|:--|:--|:--|:--|:--|
| A | サブアリーナ | 3面 | バドミントン1・2・3 | 9〜17時（または終日） |
| B | メインアリーナ（全面） | 4面 | バドミントン1・2・3・4 | 17〜21時（または終日） |
| C | メインアリーナ（一部専用利用） | 2面 | バドミントン1・2のみ（3・4は専用利用で不可） | 日によって異なる（木曜等に出現） |

---

### 2-3. 亀田総合体育館

**スケジュールページのセル表記形式**: `"9時～Aパターン 19時～Cパターン"` （開始時刻のみ。終了時刻は記載なし）

> ⚠️ 終了時刻は F1 スクレイピング時に計算して埋める（D-024）：次のパターンの開始時刻を終了時刻とする。その日の最終パターンは平日21:00・日祝17:00。

| パターン | 使える体育室 | バドミントン面数 | コート番号 | 時間帯 |
|:--|:--|:--|:--|:--|
| A | サブアリーナ（全面） | 3面 | バドミントン9・10・11 | 日によって異なる |
| B | サブアリーナ（一部） | 2面 | バドミントン10・11のみ（9のスペースは卓球台として使用） | 日によって異なる |
| C | メインアリーナ（一部） | 4面 | バドミントン1・2・5・6 | 日によって異なる |
| D | サブアリーナ＋メインアリーナ 全面 | 11面 | バドミントン1〜11（サブ：9・10・11 / メイン：1〜8） | 日によって異なる |

> ⚠️ C パターンのコート番号が連番でない理由：メインアリーナは 1・2・5・6 と 3・4・7・8 に分かれており、C パターンでは前者のみ使用。後者（3・4・7・8）は大会・専用利用時に追加開放（D パターン）。
>
> ⚠️ D パターンの 3・4・7・8 は現在（2026-06）予約ページで選択不可。D パターン開放日には全コート使用可と推測されるが要実機確認。

---

## 3. LIFF 画面仕様

> 対象ファイル：`docs/` フォルダの HTML ファイル（GitHub Pages 経由で LIFF が読み込む）

### 3-1. 日程回答フォーム（docs/liff.html）

**表示条件：**
- reserver-master に lineUserId が存在する → フォームを表示
- reserver-master に lineUserId が存在しない → 「このサービスは登録メンバー専用です」を表示してフォーム非表示

**画面構成：**

| 要素 | 種類 | 内容 |
|:--|:--|:--|
| ヘッダー | テキスト | 「〇月のバドミントン日程」（月名は動的） |
| 施設タブ | タブ切り替え | 鳥屋野 / 東 / 亀田（3タブ） |
| 日程スロット一覧 | 繰り返し行 | 日付・時間帯・開放パターン・「行ける」ボタン・「未定」ボタン |
| 送信ボタン | ボタン | 「送信する」 |
| ローディング表示 | スピナー | 送信中に表示 |

**エラー表示：**

| エラー条件 | 表示メッセージ |
|:--|:--|
| 未登録ユーザーがアクセス | 「このサービスは登録メンバー専用です」 |
| 送信失敗（サーバーエラー） | 「送信できませんでした。もう一度お試しください」 |
| ロック競合（混雑） | 「サーバーが混んでいます。少々お待ちのうえ再送信ください」 |

---

### 3-2. 回答状況一覧（docs/liffResults.html）

**画面構成：**

| 要素 | 種類 | 内容 |
|:--|:--|:--|
| ヘッダー | テキスト | 「参加状況」 |
| 日時スロット一覧 | 繰り返し行 | 日付・時間帯・施設・「行ける」人数・参加者名（表示名） |
| 更新日時 | テキスト | 「最終更新：〇月〇日〇時〇分」 |

---

### 3-3. 予約フォーム（docs/reserve.html）

**表示条件：**
- reserver-master に lineUserId が存在し user_type = reserver → フォームを表示
- reserver-master に lineUserId が存在しない → 「このサービスは登録メンバー専用です」を表示
- user_type = answer_only → 「予約操作は予約者登録が必要です。管理者に連絡してください」を表示

**画面の流れ（ステップ）：**

```
ステップ1：予約情報の確認
  → 日時・施設が表示される（URL パラメータから取得）

ステップ2：コート選択
  → 施設ごとのコート一覧（第1〜第3希望ドロップダウン）
  → 体育室レイアウト図 or 施設HPリンク（著作権確認後に決定）

ステップ3：予約者情報の確認
  → 氏名・電話・メール（reserver-master から自動入力）
  → 未登録の場合は入力フォームを表示

ステップ4：確認画面
  → 入力内容の最終確認 → 「予約する」ボタン

ステップ5：完了画面
  → 「予約リストに追加しました」 or 「予約が完了しました」
```

**エラー表示：**

| エラー条件 | 表示メッセージ |
|:--|:--|
| 未登録ユーザー | 「このサービスは登録メンバー専用です」 |
| 回答のみユーザー | 「予約操作は予約者登録が必要です。管理者に連絡してください」 |
| IDトークン失効 | 「セッションが切れました。LIFFを開き直してください」 |
| 予約失敗 | 「予約に失敗しました。もう一度お試しください」 |

---

### 3-4. フル登録フォーム（docs/register_full.html）

> F6-α の「フル登録」用。氏名・電話・メールを入力して予約者として登録する。

**入力項目：**

| 項目 | 種類 | 必須 | バリデーション |
|:--|:--|:--|:--|
| 氏名 | テキスト入力 | ✅ | 1文字以上 |
| 電話番号 | テキスト入力 | ✅ | 数字・ハイフンのみ・10〜11桁 |
| メールアドレス | テキスト入力 | ✅ | メール形式（@を含む） |

---

### 3-5. 簡易登録フォーム（docs/register_simple.html）

> F6-α の「簡易登録」用。氏名のみ入力して回答のみ参加者として登録する。

**入力項目：**

| 項目 | 種類 | 必須 | バリデーション |
|:--|:--|:--|:--|
| 氏名 | テキスト入力 | ✅ | 1文字以上 |

---

## 4. LINE 通知文面カタログ

> LINE に送る全メッセージのテンプレートを一覧にまとめる。変数は `{変数名}` で表記。

### 4-1. フォーム配信（F2）

| ID | タイミング | 文面 |
|:--|:--|:--|
| MSG-F2-01 | 全施設スケジュール揃い | 「{月}月のバドミントン日程が揃いました！\n回答はこちら → {URL}" |
| MSG-F2-02 | 月20日期限切れ（未公開施設あり） | 「{月}月のバドミントン日程です（{未公開施設名}は未公開のため除外）\n回答はこちら → {URL}」 |

### 4-2. 参加状況通知（F3）

| ID | タイミング | 文面 |
|:--|:--|:--|
| MSG-F3-01 | 4人揃い（予約者あり） | 「{日付} {開始時刻}〜、{人数}人集まりました！\n対象施設：{施設名}\n予約はこちら → {予約URL}」 |
| MSG-F3-02 | 4人揃い（予約者なし） | 「{日付} {開始時刻}〜、{人数}人集まりました！\nただし予約者が誰もいません。予約者の方に代理予約を依頼するか、参加者の中から予約者として登録してください。」 |
| MSG-F3-03 | 5人目・6人目追加 | 「{日付} {開始時刻}〜、{人数}人になりました！」 |
| MSG-F3-04 | キャンセルで3人以下に | 「{日付} {開始時刻}〜の参加者が{人数}人に減りました。予約は維持されています。管理者は予約のキャンセルや追加募集を判断してください。」 |

### 4-3. 予約フロー（F4・F5）

| ID | タイミング | 文面 |
|:--|:--|:--|
| MSG-F4-01 | 予約キュー登録完了 | 「{氏名}さんの名前で{日付} {開始時刻}〜を予約リストに追加しました（{施設名}）。予約解禁日：{解禁日}」 |
| MSG-F5-01 | 即時予約成功 | 「{氏名}さんの名前で{日付} {開始時刻}〜の予約が完了しました（{施設名}）」 |
| MSG-F5-02 | 予約失敗 | 「{施設名}の予約に失敗しました。手動で予約をお試しください。（{日付} {開始時刻}〜）」 |
| MSG-F5-03 | 施設側キャンセル検知 | 「{日付} {開始時刻}〜の予約が施設側でキャンセルされた可能性があります。施設に確認してください。（{施設名}）」 |

### 4-4. メンバー登録・退出（F6）

| ID | タイミング | 文面 |
|:--|:--|:--|
| MSG-F6-01 | フル登録完了 | 「{表示名}さんが予約者として登録しました」 |
| MSG-F6-02 | 簡易登録完了 | 「{表示名}さんが回答のみ参加者として登録しました」 |
| MSG-F6-03 | メンバー退出（自動削除） | 「{表示名}さんのデータを削除しました」 |
| MSG-F6-04 | 管理者コマンドで削除 | 「{氏名}さんのデータを削除しました」 |
| MSG-F6-05 | 月次90日チェック | 「最終回答から90日以上経過しているメンバー：\n{メンバー名一覧}\n削除する場合は /削除 <ニックネーム> で実行してください。」 |

### 4-5. Welcomeメッセージ（F6-α）

| ID | タイミング | 文面 |
|:--|:--|:--|
| MSG-F6A-01 | Bot が join したとき | 「バドミントン予約Botです！参加登録をお願いします。\n予約もする方（氏名・電話・メール）→ {フル登録URL}\n日程回答のみの方（氏名のみ）→ {簡易登録URL}」 |
| MSG-F6A-02 | 新メンバーが memberJoined したとき | 「{表示名}さん、参加登録はこちら。\n予約もする方 → {フル登録URL}\n日程回答のみの方 → {簡易登録URL}」 |

### 4-6. エラー・警告通知

| ID | タイミング | 宛先 | 文面 |
|:--|:--|:--|:--|
| MSG-ERR-01 | スクレイピング失敗 | グループ | 「スケジュール取得に失敗しました。前回のデータを保持しています。（{施設名}）」 |
| MSG-ERR-02 | ロック競合集約 | グループ | 「サーバー混雑によるロック競合が{件数}件発生しました」 |
| MSG-ERR-03 | 未送信通知キュー発生 | グループ | 「通知{件数}件が未送信です。スプレッドシートの送信失敗キューを確認してください。」 |
| MSG-ERR-04 | 月150通超え警告 | グループ | 「LINE月間通知数が150通を超えました。残り{残数}通。」 |
| MSG-ERR-05 | 月200通到達（縮退） | グループ（リプライ） | 「月通知上限到達。次月1日まで通知が縮退します。最重要通知（4人到達・予約完了）は継続。」 |

---

## 5. 処理フロー詳細

### 5-1. F1 スクレイピングフロー

```
1. GASタイムトリガーが午前9〜10時に発火
2. スプレッドシートの IMPORTHTML 関数が施設HPを再取得（自動）
3. GAS が schedules シートを読み取り、データの有無を確認
4. データあり → 正常終了
5. データなし / #N/A エラー → 前回データを保持したまま
   → MSG-ERR-01 をグループに送信
   → 10分以内に完了すること
```

### 5-2. F2 フォーム配信フロー（自動トリガー）

```
1. GASタイムトリガーが毎日発火
2. schedules シートを確認し、3施設分のデータが揃っているか判定
   - 揃っている → 配信済みフラグを確認 → 未配信なら MSG-F2-01 を送信
   - 揃っていない → 今日が20日以降か確認
     - 20日以降 → 未公開施設を除外して MSG-F2-02 を送信
     - 20日未満 → 何もしない（翌日再チェック）
3. 配信済みフラグを立てる（当月内の重複配信防止）
```

### 5-3. F3-a 4人揃い通知フロー

```
1. LIFF からの回答送信リクエストを受信
2. LockService.getScriptLock() で5秒間ロックを取得
   - 失敗 → MSG-ERR（ロック競合）をユーザーに返す
3. responses シートに回答を書き込み
4. 同スロットの「行ける」回答数を集計
5. 集計数 >= 4 かつ notified_at が NULL → 通知フローへ
   - 予約者が1人以上いる → MSG-F3-01 を送信
   - 予約者が0人 → MSG-F3-02 を送信
   - notified_at に現在日時を書き込む
6. 集計数 = 5 or 6 → MSG-F3-03 を送信
7. ロック解放
```

### 5-4. F4 予約フロー（コート選択〜キュー登録）

```
1. 予約ボタンタップ → LIFF（docs/reserve.html）を開く
2. IDトークンを GAS サーバーに送信・検証
3. reserver-master からユーザー情報を取得
   - 未登録 → アクセス拒否
   - answer_only → 予約不可メッセージ表示
4. コート選択画面を表示
5. ユーザーがコートを選択して確定
6. 利用日まで7日以内？
   - Yes → Lambda に即時予約リクエストを送信
   - No → queue シートに登録・MSG-F4-01 を送信
```

### 5-5. F5 キュー処理フロー（予約解禁日の朝7時）

```
1. AWS EventBridge が朝7時に Lambda を起動
2. Lambda が GAS の queue エンドポイントに status=waiting のエントリを要求
3. 各エントリに対して：
   a. unlock_date が今日以前か確認
   b. コート優先順位（第1→第2→第3→空き）の順で予約フォームを送信
   c. 成功 → queue.status = done・MSG-F5-01 を送信
   d. 失敗 → queue.status = failed・MSG-F5-02 を送信
4. 7時〜7時10分の間に完了（土日の予約争奪を考慮）
```

### 5-6. 状態遷移（queue.status）

```
[waiting] → 予約実行成功 → [done]
[waiting] → 予約実行失敗 → [failed]
[waiting] → 手動キャンセル（管理者操作） → [cancelled] ※将来実装
```

---

## 6. API インターフェース

### 6-1. GAS の doPost エンドポイント

GAS は LINE Webhook と LIFF からの POST リクエストを `doPost(e)` で受け取る。

| パス / パラメータ | 送信元 | 処理 |
|:--|:--|:--|
| `action=answer` | LIFF（日程回答） | responses シートに回答を書き込む |
| `action=reserve` | LIFF（予約フォーム） | queue シートにエントリを追加する |
| `action=register_full` | LIFF（フル登録） | reserver-master に予約者として登録する |
| `action=register_simple` | LIFF（簡易登録） | reserver-master に回答のみとして登録する |
| `action=verify_token` | LIFF（共通） | LINE の /oauth2/v2.1/verify に問い合わせてIDトークンを検証する |
| （LINE Webhook） | LINE Platform | イベント種別を判定して各ハンドラに振り分ける |

### 6-2. GAS → Lambda インターフェース

| 項目 | 内容 |
|:--|:--|
| プロトコル | HTTPS POST |
| 認証 | TBD（APIキー or IAM 署名） |
| リクエスト形式 | JSON |
| リクエスト例 | `{ "date": "2026-07-05", "start_time": "13:00", "facility": "鳥屋野総合体育館", "court": "第1コート", "name": "田中太郎", "phone": "090-xxxx-xxxx", "email": "xxx@example.com" }` |
| レスポンス | `{ "success": true }` or `{ "success": false, "error": "エラーメッセージ" }` |

---

## TBD（スクレイピング検証後に記入）

以下の項目は F1（スクレイピング）を実機で動かしてから確定する。

| 項目 | 状態 | 確定後の記入場所 |
|:--|:--|:--|
| 開放パターン A/B/C/D の具体的な体育室の組み合わせ・時間帯（全3施設） | **✅ 記入済み（セクション2参照）** | セクション2 |
| 各施設のコート名（正式名称・数・並び順） | **✅ 確定済み（D-020 参照）** | セクション1-4（court_priority の値） |
| フロアマップ画像の取得方法・URL形式 | **✅ 確定済み（D-022 参照）** | セクション5-1 |
| IMPORTHTML 関数で取得する URL・セル範囲・パース方法 | TBD（F1 実装時に記入） | セクション5-1 |
| GAS → Lambda の認証方式（APIキー vs IAM 署名） | TBD（F5 実装時に記入） | セクション6-2 |

### 回答フォームの表示ルール（2026-06-02 確定 / D-023）

**未公開施設の表示**
- 公開済み施設 → パターン情報を表示
- 未公開施設 → 「（未公開）」と明示する

**時間帯ボタンのグレーアウト判定**
- 公開済み施設のうち1つでもその時間帯に使える → 押せる
- 公開済み施設がすべてその時間帯を使えない（×・休館・満） → グレーアウト
- 未公開施設は判定に含めない

**LINE 通知タイミング**
- 「全施設の予定が揃いました」通知は全施設の当該月スケジュールが揃った時点で1回だけ送る
- 通知前でも利用者は任意のタイミングで回答フォームを開くことができ、公開済み分は表示される

---

## 更新履歴

| 日付 | 変更内容 |
|:--|:--|
| 2026-06-01 | 初版作成（骨組み）。TBD はスクレイピング検証後に記入 |
| 2026-06-02 | TBD の進捗更新・回答フォーム表示ルール（D-023）を追記 |
| 2026-06-02 | セクション1を最新化：schedules に floor_map_url 追加（D-022）・court_priority TBD を確定情報に更新（D-020）・settings シートを新規追加（D-021） |
| 2026-06-02 | セクション2（開放パターン定義）を記入：D-020 の実機調査結果を転記。鳥屋野2パターン・東総合3パターン・亀田4パターン確定 |
