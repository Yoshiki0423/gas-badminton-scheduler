# 引継ぎプロンプト — gas-badminton-scheduler

最終更新：2026-06-02（F1 スクレイピング実装・動作確認完了）

---

## プロジェクトの背景と目的

新潟市の市民体育館（バドミントン個人開放）の日程調整を LINE グループ内で完結させる Bot。
体育館 HP の空き情報を自動取得し、日程アンケート配信 → 4人揃い通知 → 自動予約まで一気通貫で自動化する。
スタック：GAS + LINE Messaging API + AWS Lambda + Google スプレッドシート + GitHub Pages（LIFF）。

---

## このセッションで行ったこと（2026-06-02）

1. **SPECIFICATION.md セクション2（開放パターン定義）を完成**
2. **src/scraper.js を全面書き直し**（IMPORTHTML方式・3施設対応）
3. **デバッグ機能を作成**：`docs/debug.html` + GAS `?debug=scrape_preview&token=XXX` エンドポイント
4. **スクレイピング動作確認**（デバッグページで実際のデータ取得を確認済み）
5. **バグ修正3件**：
   - 時間指定なし（終日）セルのフォールバック追加（D-025）
   - rowspan 継続行（col0 空白）を同日扱いに修正
   - `split('/')` → グローバル正規表現化（改行区切り対応・D-026）
6. **DECISION_NOTES.md に D-025・D-026 を追記**
7. **サイト実データとの照合**（WebFetch で3施設確認）

---

## 次のセッションでやること

### Step 0（セッション開始直後）：スクレイピング確認事項の回答を受け取る

Yoshiki が以下3点を確認して回答してくれる予定：
1. 東総合 6/11・18・25 に **C パターンが3行目として出ているか**
2. 鳥屋野 6/27 の「パターン文字なし」エントリの扱い（取得できなくてよいか確認）
3. 亀田 6/11 の `9時〜15時Aパターン`（特殊形式）の end_time が実際と合っているか

### Step 1：schedules シートへの書き込み確認

```
GAS エディタで scrapeAllFacilities() を実行
→ schedules シートにデータが書き込まれることを確認
```

### Step 2：タイムトリガー設定

```
GAS エディタで setupScraperTrigger() を実行（1回だけ）
→ 毎日午前9時に scrapeAllFacilities() が自動実行される
```

### Step 3：F2（アンケート配信）の実装に着手

---

## 注意事項

- **REQUIREMENTS.md は勝手に修正しない**（指示があってから）
- **clasp push は Yoshiki が直接実行する**（`clasp push --force` と案内する）
- **LINE Bot・LIFF 設定の説明は1ステップずつ丁寧に**（設定内容を把握していない）
- **指示なしに実装・修正を実行しない**
- **コート名は全施設「バドミントンX」形式**（「コートX」は誤り）

---

## 読むべきファイル（優先順）

| ファイル | 読む理由 |
|:--|:--|
| `SPECIFICATION.md` | 仕様書。セクション1・2完成済み |
| `REQUIREMENTS.md` | 要件書（確定済み）。F1〜F7・データモデル・AC |
| `DECISION_NOTES.md` | D-001〜D-026 の設計判断。D-025・D-026 が最新 |
| `src/scraper.js` | F1 実装済みコード |
| `docs/debug.html` | スクレイピング確認用デバッグページ |
